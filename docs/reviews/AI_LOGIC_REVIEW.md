# AI 로직 리뷰 리포트 (퀀트 관점)

**날짜**: 2026-01-02
**리뷰어**: Antigravity (Google Deepmind)
**범위**: AI 프롬프트 엔지니어링 및 신호 분석 로직 (src/ai/service.py)

## 1. 강점 (Pros)

### ✅ "리스크 헌터(Risk Hunter)" 페르소나
`src/ai/service.py`에 구현된 프롬프트 엔지니어링은 탁월합니다.
- **검증적 사고방식**: "당신의 임무는 이 거래를 **막을 이유를 찾는 것**입니다."라는 지시는 전문적인 리스크 관리 원칙과 완벽하게 일치합니다. 아마추어는 '진입할 이유'를 찾지만, 프로는 '진입하지 말아야 할 이유'를 찾습니다.
- **명시적 제약**: "이유가 하나라도 있다면 HOLD를 고려하라"는 보수적인 편향(Conservative Bias)은 자동매매 시스템에 매우 적절합니다.

### ✅ 시장 국면(Regime) 인식
프롬프트에 **"Regime Analysis"**를 명시적으로 주입하고 있습니다:
- *"현재의 변동성/거래량이 백테스트 기간과 유사합니까?"*
- 이는 퀀트 전략의 가장 큰 실패 원인인 **'국면 전환(Regime Change)'** 문제를 정면으로 다룹니다. 강세장에서 작동하던 전략이 횡보/하락장에서 망가지는 것을 방지하기 위해, AI에게 "시장 환경의 불일치"를 감지하도록 지시한 점은 매우 훌륭합니다.

### ✅ 풍부한 데이터 컨텍스트 (Data-Rich Context)
LLM에게 단순 가격뿐만 아니라 방대한 양의 퀀트 데이터를 제공하고 있습니다:
- `daily_chart_summary` (일봉 요약)
- `advanced_orderbook` (매수/매도벽, 고래 주문 분석)
- `risk_metrics` (샤프 비율, 변동성)
- `market_structure` (피보나치, 지지/저항)
이는 LLM이 단순한 '찍기 머신'이 아니라, 다양한 신호를 종합(Synthesizer)하는 분석가 역할을 수행하게 합니다.

## 2. 약점 (Cons)

### ⚠️ 확률적 LLM에 대한 결정론적 로직 의존
- **문제점**: 복잡한 수치 계산이나 해석(예: "이 피보나치 레벨들을 분석해라")을 LLM에게 과도하게 의존합니다.
- **위험**: LLM은 공간 추론이나 정밀한 산수에는 약합니다. "거래량 비율이 1.5배 이상인가?" 같은 판단은 파이썬 코드에서 계산하여 `is_high_volume=True` 같은 Boolean 플래그로 넘겨주는 것이 훨씬 안전합니다.
- **환각(Hallucination)**: 프롬프트가 다이버전스를 찾으라고 강하게 유도하면, 실제로는 없는데도 억지로 "다이버전스가 보인다"고 환각을 일으킬 수 있습니다.

### ⚠️ 프롬프트 유지보수성
- **문제점**: 거대한 프롬프트가 파이썬 코드 내부에 긴 f-string으로 하드코딩 되어 있습니다.
- **결과**: 프롬프트 자체에 대한 버전 관리(Git)가 어렵습니다. 다양한 페르소나를 A/B 테스트하거나, 코더가 아닌 사람이 프롬프트를 수정하기가 어렵습니다.

### ⚠️ 구조화된 스코어링 부재
- **문제점**: AI가 JSON으로 `decision`, `reason`, `confidence`를 반환하지만, `reason`은 비구조화된 텍스트입니다.
- **퀀트적 필요**: 정량적인 **하위 점수(Sub-scores)**가 필요합니다. 예: `trend_score: 8`, `risk_score: 2`, `macro_score: 5`. 이렇게 해야 파이썬 계층에서 "Trend > 7 이고 Risk < 4 일 때만 매수"와 같은 가중치 기반 앙상블 로직을 짤 수 있습니다.

## 3. 개선 사항 및 제언

### 🔧 1. 구조화된 출력 (Chain of Thought Blocks)
**우선순위**: 높음
**조치**: JSON 스키마를 수정하여 하위 구성 요소별 점수를 보고받으세요.
```json
{
  "scores": {
    "trend": 80,
    "valuation": 40,
    "risk": 20
  },
  "flags": {
    "regime_change_detected": false,
    "fakeout_risk": true
  },
  "final_decision": "HOLD"
}
```
이 데이터를 DB에 저장하면 나중에 **AI 자체의 성과**를 정량적으로 분석할 수 있습니다.

### 🔧 2. 신호의 사전 계산 (Pre-Computation)
**우선순위**: 중간
**조치**: "해석"의 영역을 파이썬으로 옮기세요.
- 시가/고가/저가/종가 원본 데이터를 그대로 주는 대신, 파이썬에서 계산된 요약된 기술어(Descriptor)를 제공하세요.
- 예: `"trend": "strong_uptrend"`, `"volatility": "contracting"`.
- 이는 토큰 비용을 절약하고, LLM의 계산 오류/부하를 줄여줍니다.

### 🔧 3. 퓨샷 프롬프팅 (Few-Shot Prompting)
**우선순위**: 중간
**조치**: "모범적인 거절 사례" 1~2개를 예시로 프롬프트에 주입하세요.
- 지표는 좋았지만 컨텍스트(예: 다이버전스)가 나빠서 "가짜 돌파(Fakeout)"로 판명된 사례를 보여주고, 프로 트레이더가 이를 어떻게 거절했는지 예시를 보여주세요. 단순한 지시보다 예시를 보여주는 것이 모델의 추론 패턴을 훨씬 더 강력하게 제어합니다.
